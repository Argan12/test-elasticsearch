@model Elasticsearch.Models.Artist

<div class="text-right mb-3">
    <a href="@Url.Action("Edit", "Artist", new { id = @Model.Id })" class="btn btn-primary">Modifier</a>
    <a href="#" class="btn btn-danger" data-toggle="modal" data-target="#deleteArtist">Supprimer</a>
</div>

<h2>@Model.Name</h2>

<p>@Model.Description</p>

<small>@Model.Genres</small>

<div class="modal fade" id="deleteArtist" tabindex="-1" role="dialog" aria-labelledby="deleteArtist" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Supprimer un artiste</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Voulez-vous vraiment supprimer l'artiste &laquo; @Model.Name &raquo; ?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Fermer</button>
        <a href="@Url.Action("Delete", "Artist", new { id = @Model.Id })" class="btn btn-danger">Supprimer</a>
      </div>
    </div>
  </div>
</div>